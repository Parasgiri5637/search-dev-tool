name: flutter_code_map_analyzer
description: Core AST analysis, knowledge graph, and query engine for Flutter Code Map.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: '>=6.0.0 <7.0.0'
  path: ^1.9.0
  meta: ^1.11.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
